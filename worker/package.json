{
  "name": "tripspend-ai-worker",
  "private": true,
  "type": "module",
  "version": "7.1.0",
  "scripts": {
    "test": "node ai-worker-contract.test.mjs",
    "deploy": "npx --yes wrangler@latest deploy",
    "deploy:login": "npx --yes wrangler@latest login && npx --yes wrangler@latest deploy"
  }
}
