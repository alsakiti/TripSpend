/* =========================================================
   TripSpend v6.7.6 — Calm Home Dashboard
   Home = spending status. Plan = full planning.
   Pure UI release; no data-model changes.
   ========================================================= */

.v676-dashboard{
  --home-gap:12px;
  display:flex;
  flex-direction:column;
  gap:var(--home-gap);
}
.v676-dashboard>.v6-budget-card,
.v676-dashboard>.v6-quick-add,
.v676-dashboard>.section,
.v676-dashboard>.health-banner,
.v676-dashboard>.home-trip-history-btn{
  margin-top:0!important;
  margin-bottom:0!important;
}

/* 1 — Budget is the hero, but with less "card stack" weight. */
.v676-budget-wrap{
  display:grid;
  gap:8px;
}
.v676-budget-hero{
  border-radius:21px!important;
  padding:20px!important;
  box-shadow:0 12px 30px rgba(15,23,42,.12)!important;
}
.v676-budget-hero .summary-head{
  align-items:flex-start;
}
.v676-budget-hero .remaining strong{
  line-height:.98;
}
.v676-budget-hero .progress{
  margin:17px 0 16px!important;
  height:7px!important;
}
.v676-budget-small{
  display:grid!important;
  grid-template-columns:1fr 1fr;
  gap:14px!important;
}
.v676-budget-small>span{
  min-width:0;
}
.v676-budget-small>span:last-child{
  text-align:right;
}
.v676-budget-small small{
  display:block!important;
  margin-bottom:3px;
  color:#b9c2d0!important;
  font-size:9px!important;
  text-transform:uppercase;
  letter-spacing:.055em;
}
.v676-budget-small strong{
  display:block;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
  font-size:12px!important;
}

/* 2 — Safe Today and Spent Today are one compact strip, not two cards. */
.v676-day-strip{
  display:grid;
  grid-template-columns:minmax(0,1fr) 1px minmax(0,1fr);
  align-items:center;
  min-height:68px;
  padding:8px 12px;
  border:1px solid var(--line);
  border-radius:16px;
  background:var(--surface);
}
.v676-day-divider{
  width:1px;
  height:36px;
  background:var(--line);
}
.v676-day-stat{
  min-width:0;
  padding:4px 8px;
}
.v676-day-stat:last-child{
  text-align:right;
}
.v676-day-label{
  display:flex;
  align-items:center;
  gap:5px;
  color:var(--muted);
  font-size:9px;
  font-weight:750;
}
.v676-day-stat:last-child .v676-day-label{
  justify-content:flex-end;
}
.v676-day-stat strong{
  display:block;
  margin-top:4px;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
  font-size:15px;
  letter-spacing:-.025em;
  font-variant-numeric:tabular-nums;
}
.metric-info{
  width:17px!important;
  height:17px!important;
  min-height:17px!important;
  padding:0!important;
  border:1px solid currentColor!important;
  border-radius:999px!important;
  background:transparent!important;
  color:inherit!important;
  font-size:9px!important;
  line-height:1!important;
  opacity:.62;
}
